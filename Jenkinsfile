pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello from Pipeline!'
                echo "Build Number: ${BUILD_NUMBER}"
            }
        }
    }
}